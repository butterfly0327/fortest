<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.backend.mapper.GestureMapper">

    <resultMap id="GestureResultMap" type="com.backend.backend.Entity.Gesture">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="imageUrl" column="image_url"/>
    </resultMap>


    <select id="findAll" resultMap="GestureResultMap">
        SELECT id, code, name, description, image_url
        FROM gesture
    </select>


    <select id="findById" resultMap="GestureResultMap">
        SELECT id, code, name, description, image_url
        FROM gesture
        WHERE id = #{id}
    </select>

    <select id="findByCode" resultMap="GestureResultMap">
        SELECT id, code, name, description, image_url
        FROM gesture
        WHERE code = #{code}
    </select>

    <insert id="insert" parameterType="com.backend.backend.Entity.Gesture"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO gesture (code, name, description, image_url)
        VALUES (#{code}, #{name}, #{description}, #{imageUrl})
    </insert>

    <update id="update" parameterType="com.backend.backend.Entity.Gesture">
        UPDATE gesture
        SET code = #{code},
            name = #{name},
            description = #{description},
            image_url = #{imageUrl}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM gesture
        WHERE id = #{id}
    </delete>

</mapper>
