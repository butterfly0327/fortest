<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.backend.mapper.MappingMapper">

    <resultMap id="MappingResultMap" type="com.backend.backend.Entity.Mapping">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="isActive" column="is_active"/>
        <result property="changeType" column="change_type"/>
        <result property="note" column="note"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <association property="account" javaType="com.backend.backend.Entity.Account">
            <id property="id" column="account_id"/>
        </association>
    </resultMap>


    <select id="findAll" resultMap="MappingResultMap">
        SELECT id, account_id, name, is_active, change_type, note, created_at, updated_at
        FROM mapping
    </select>


    <select id="findById" resultMap="MappingResultMap">
        SELECT id, account_id, name, is_active, change_type, note, created_at, updated_at
        FROM mapping
        WHERE id = #{id}
    </select>

    <select id="findByAccountId" resultMap="MappingResultMap">
        SELECT id, account_id, name, is_active, change_type, note, created_at, updated_at
        FROM mapping
        WHERE account_id = #{accountId}
    </select>

    <select id="findActiveByAccountId" resultMap="MappingResultMap">
        SELECT id, account_id, name, is_active, change_type, note, created_at, updated_at
        FROM mapping
        WHERE account_id = #{accountId} AND is_active = true
    </select>

    <insert id="insert" parameterType="com.backend.backend.Entity.Mapping"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mapping (account_id, name, is_active, change_type, note, created_at, updated_at)
        VALUES (#{account.id}, #{name}, #{isActive}, #{changeType}, #{note}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.backend.backend.Entity.Mapping">
        UPDATE mapping
        SET account_id = #{account.id},
            name = #{name},
            is_active = #{isActive},
            change_type = #{changeType},
            note = #{note},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM mapping
        WHERE id = #{id}
    </delete>

</mapper>
